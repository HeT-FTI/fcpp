# Checkov skips for the advisory MegaLinter layer; the required gates are native + SonarCloud
skip-check:
  # Job-level permissions satisfy SonarCloud S8264 -- do not re-add workflow-level
  - CKV2_GHA_1
  # workflow_dispatch inputs select board targets; values are allow-listed in the workflow
  - CKV_GHA_7
